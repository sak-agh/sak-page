import React from 'react';

interface Props {
  children?: React.ReactNode;
}

const Dashboard = (props: Props) => {
  return (
    <>
      <h1>Dasboard</h1>
    </>
  );
};

export default Dashboard;
